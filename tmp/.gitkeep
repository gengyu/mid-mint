# Temporary Files

此目录用于存放临时文件，不会被 Git 跟踪。

## 目录结构

```
tmp/
├── renders/        # Slide 渲染中间产物
├── exports/        # 导出临时文件
├── uploads/        # 上传文件临时存储
└── previews/       # 预览图
```

## 注意事项

- 此目录下的文件会被 `.gitignore` 忽略
- 应用启动时会自动创建必要的子目录
- 定期清理旧文件以节省空间
